#ifndef __EUCLIDEAN__
#define __EUCLIDEAN__

#include <vector>

// Function to caclulate euclidean distance between two vectors
double EuclideanDistance(const std::vector<float>&, const std::vector<float>&);

#endif